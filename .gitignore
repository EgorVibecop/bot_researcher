.env
__pycache__/
*.pyc
venv/
*.db
bot_log.txt

# Файл сессии Telegram — это доступ к аккаунту, ему в репозитории не место.
# Сейчас не создаётся (каналы читаются без аккаунта), оставлено на всякий случай.
*.session
*.session-journal
